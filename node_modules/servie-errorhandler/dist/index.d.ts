import { Request, Response } from "servie/dist/node";
export interface Options {
    production?: boolean;
    log?: (err: any) => void;
}
/**
 * Render errors into a response object.
 */
export declare function errorhandler(req: Request, options?: Options): (err: any) => Response;
